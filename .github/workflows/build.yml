name: Build .NET Standard projects

on:
  - workflow_dispatch
  - push
  - pull_request

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: |
        cd src
        ./netStandard.bat
